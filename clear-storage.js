// This would normally clear AsyncStorage but since it's React Native specific,
// we just need to restart the app after the code changes
console.log('AsyncStorage will be cleared on next app restart with the new code')
console.log('The loadStore function will now sync with Supabase data on first load')
